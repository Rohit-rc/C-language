#include<stdio.h>
#include<conio.h>
void main()
{
int num,count=0;
clrscr();
printf("Enter the Number : ");
scanf("%d",&num);
for (int i=num ; i>0 ; i=i/10)
	count++;
printf("%d Number of digits",count);
getch();
}
